# AVISO: Erro de tipo no original - operador < entre BOOL e INT.
# Em Icon nao ha verificacao de tipo; a comparacao numerica e realizada.
procedure erroMenorBool(ativo, limite)
  local resultado
  if ativo < limite then {
    resultado := 1
  } else {
    resultado := 0
  }
  return resultado
end
