# AVISO: Erro de tipo no original — comparacao == entre INT e BOOL.
# Em Icon nao ha verificacao de tipo; a comparacao numerica e realizada.
procedure erroIgualdadeTiposMistos(x, b)
  local resultado
  resultado := if x = b then 1 else 0
  return resultado
end
