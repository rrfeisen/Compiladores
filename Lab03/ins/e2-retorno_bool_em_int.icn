# AVISO: Erro de tipo no original - funcao declarada como int, mas retorna bool.
# Em Icon, bool e representado como 1/0.
procedure erroRetornoBool(x)
  local flag
  if x < 0 then {
    flag := 1
  } else {
    flag := 0
  }
  return flag
end
